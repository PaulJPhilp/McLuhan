{
  "name": "mcluhan",
  "version": "0.0.0",
  "private": true,
  "description": "McLuhan monorepo: Effect-native packages for AI agent infrastructure",
  "type": "module",
  "packageManager": "bun@1.1.33",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "typecheck": "bun run --filter '*' typecheck",
    "test": "bun run --filter 'effect-*' test",
    "test:watch": "bun run --filter 'effect-*' test:watch",
    "lint": "bun run --filter '*' lint",
    "format": "bun run --filter '*' format",
    "format:fix": "bun run --filter '*' format:fix",
    "verify": "bun run format && bun run lint && bun run typecheck && bun run build && bun run test",
    "verify:ci": "bun run lint && bun run typecheck && bun run build"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.7",
    "typescript": "^5.9.3"
  }
}